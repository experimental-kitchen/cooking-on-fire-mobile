import {SlashSeparatedListPipePipe} from './slash-separated-list.pipe';

describe('CategoryListPipe', () => {
  it('create an instance', () => {
    const pipe = new SlashSeparatedListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
